USE_OCAMLFIND = true

OCAMLPACKS[] =
    batteries

OCAMLINCLUDES[] =
    ../poly

OCAMLFLAGS += -thread

NATIVE_ENABLED = true
BYTE_ENABLED = true

OCAML_LIBS[] =
    ../poly/poly

FILES[] =
    ConstraintTypes
    ConstraintImpl
    Atoms
    Constraints

LIB = constraints

OCamlLibrary($(LIB), $(FILES))

clean:
  rm -f \
     *.omc \
     $(filter-proper-targets $(glob $(addsuffix .*, $(FILES)))) \
     $(LIB).a $(LIB).cmxa $(LIB).cma \
     *~
